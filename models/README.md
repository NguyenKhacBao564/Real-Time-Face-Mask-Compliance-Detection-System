# Models Directory

Store trained weights and exported models locally here.

Expected files:

```text
models/best.pt
models/best.onnx
```

Model files are ignored by Git. Put download links, release links, or Hugging Face model repo links in this file after training.

